---
import Footer from "../components/footer.astro";
import Header from "../components/header.astro";
import "../assets/css/main.css";
const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>

    <script is:inline>
      if (typeof Storage !== 'undefined') {
        if (
          localStorage.getItem('dark_mode') &&
          localStorage.getItem('dark_mode') == 'true'
        ) {
          document.documentElement.classList.add('dark')
        }
      }
    </script>

    <style>
      .prose img {
        border-radius: 20px;
      }
    </style>
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />

    <!-- GoatCounter analytics. Requests from localhost and private networks
         are ignored by count.js, so `npm run dev` does not affect stats.
         Append #toggle-goatcounter to a URL to mute your own visits. -->
    <script
      is:inline
      data-goatcounter="https://krishnkant.goatcounter.com/count"
      async
      src="https://gc.zgo.at/count.js"></script>

    <Fragment set:html={import.meta.env.HEADER_INJECT} />
  </head>
  <body class="antialiased bg-white dark:bg-neutral-950">
    <Header />
    <slot />
    <Footer />
    <script src="/assets/js/main.js" defer></script>
    <Fragment set:html={import.meta.env.FOOTER_INJECT} />
  </body>
</html>
